/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.up.rebirth',
  appName: 'Up Rebirth',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
